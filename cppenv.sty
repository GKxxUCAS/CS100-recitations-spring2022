\usepackage{listings}

\lstset{
    columns = fixed,       
    % numbers = left,                                      % show line number on the left
    breaklines = true,
    backgroundcolor = \color{white},                       % set the background color
    keywordstyle = \color[RGB]{40,40,255},                 % set the keyword color
    numberstyle = \footnotesize\color{darkgray},           % set the style of line numbers
    commentstyle = \color[RGB]{2,128,9},                   % set the style of comments
    basicstyle = \small\ttfamily,
    stringstyle = \ttfamily\color[RGB]{128,0,0},           % set the style of string literals
    showstringspaces = false,                              % hide the spaces in string literals
    language = {[11]C++},
}

\lstnewenvironment{cpp}{\lstset{language = {[11]C++}}}{}